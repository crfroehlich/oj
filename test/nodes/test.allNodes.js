// Generated by CoffeeScript 1.7.1
(function() {
  (function(OJ) {
    module('all nodes', {
      setup: function() {
        OJ['GENERATE_UNIQUE_IDS'] = true;
        if (!OJ.body.make) {
          return OJ.nodes.div();
        }
      }
    });
    test('Test all nodes', function() {
      var count;
      count = 0;
      OJ.each(OJ.nodes.members, function(val) {
        if (val !== 'factory' && val !== 'get' && val !== 'makeSubNameSpace' && val !== 'constants') {
          return count += 1;
        }
      });
      expect(count * 7);
      OJ.each(OJ.nodes.members, function(val, key) {
        var cNode, child, childId, dNode, node, nodeId;
        if (val !== 'factory' && val !== 'get' && val !== 'makeSubNameSpace' && val !== 'constants') {
          node = OJ.body.make(val);
          node.text('Hi I\'m some text');
          deepEqual(node.tagName, val, 'Node is a ' + val.toUpperCase());
          nodeId = node.getId();
          dNode = document.getElementById(nodeId);
          ok(dNode, 'Node is in the DOM');
          deepEqual(nodeId, dNode.id, 'Element IDs are equal');
          child = node.make(val);
          childId = child.getId();
          deepEqual(child.tagName, val, 'Node is a ' + val.toUpperCase());
          cNode = document.getElementById(childId);
          deepEqual(cNode.id, childId, 'Element IDs are equal');
          node.remove();
          equal(undefined, document.getElementById(nodeId), 'Node has been removed');
          return equal(undefined, document.getElementById(childId), 'Child has been removed');
        }
      });
    });
  })((typeof global !== 'undefined' && global ? global : (typeof window !== 'undefined' ? window : this)).OJ);

}).call(this);

//# sourceMappingURL=test.allNodes.map
